struct Point { x: float, y: float }

let p = Point{x = 3.0, y = 4.0} in
    print(p.x) + print(p.y)

fn area(r: matrix[1, 4])
    (r[0, 2] - r[0, 0]) * (r[0, 3] - r[0, 1])

let rect: matrix[1, 4] = [0.0, 0.0, 5.0, 10.0] in
    print(area(rect))
